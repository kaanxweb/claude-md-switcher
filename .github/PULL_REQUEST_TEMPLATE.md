## Summary

## Linked issue (if any)
Closes #

## Test plan
- [ ] Built with `./build.sh` — no warnings
- [ ] Ran `swift test` — all tests pass
- [ ] Ran `bash tests/test_release_script.sh` — all preflight checks pass
- [ ] Ran `bash tests/test_bundle_packaging.sh` — the self-contained arm64 bundle passes packaging checks
- [ ] Manually tested the relevant menu interaction (UI changes require visual testing — see `tasks/lessons.md`)

## Screenshots / GIF (for UI changes)
